import { ChildrenInterface } from '@/shared/models'
import React from 'react'

export default function AdminTeamUpdateLayout({ children }: ChildrenInterface) {
    return (
        <>{children}</>
    )
}
