export function MaterialIcon({ icon }) {
    return <span className="mat-icons material-symbols-outlined">{icon}</span>;
}
export function MaterialIconFilled({ icon }) {
    return <span className="mat-icons material-symbols-filled">{icon}</span>;
}
